# Настройка автоматического деплоя

## Секреты GitHub

Для работы автоматического деплоя необходимо настроить следующие секреты в GitHub:

### Как добавить секреты:

1. Откройте репозиторий на GitHub
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Добавьте следующие секреты:

| Имя секрета | Значение |
|------------|----------|
| `SERVER_HOST` | `69.197.178.118` |
| `SERVER_USER` | `administrator` |
| `SERVER_PASSWORD` | `Astana@2026` |

## Как работает автоматический деплой

После настройки секретов, при каждом `git push` в ветку `main`:

1. ✅ GitHub Actions автоматически запустится
2. 📥 Подключится к серверу по SSH
3. 🔄 Выполнит `git pull origin main`
4. 🔨 Пересоберёт Docker контейнеры (`docker compose up -d --build`)
5. 🧹 Очистит неиспользуемые образы
6. ✅ Проверит статус контейнеров

## Ручной запуск деплоя

Можно также запустить деплой вручную:

1. Перейдите в **Actions** → **Deploy to Production**
2. Нажмите **Run workflow**
3. Выберите ветку `main` и нажмите **Run workflow**

## Просмотр логов деплоя

1. Перейдите в **Actions** в репозитории
2. Выберите нужный workflow запуск
3. Откройте шаг **Deploy to server** для просмотра логов

## Проверка статуса

После деплоя проверьте:
- Frontend: http://69.197.178.118:5173
- Backend API: http://69.197.178.118:8000
- API Docs: http://69.197.178.118:8000/docs
